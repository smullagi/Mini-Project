package com.cg.ems.dao;

public class AuthenticationDaoImpl implements IAuthenticationDao {

	public boolean loginSuccessful(String userName, String userPassword) {
		boolean success = true;
		
		// check if the user exist in database(table: User) and verify password is correct
		
		
		return success;
	}
}
